cmake_minimum_required(VERSION 2.8)
find_package(MPI REQUIRED)
include_directories(${MPI_INCLUDE_PATH})
add_executable(mpiSharedMemoryTest main.cc)
target_link_libraries(mpiSharedMemoryTest ${MPI_LIBRARIES})
